# 🌲 빠른 숲 생성 가이드

## ❌ 문제: 커서에서 숲 생성 요청했지만 숲이 생성되지 않음

에디터 로그를 분석한 결과 다음 문제들이 발견되었습니다:

---

## 🔧 해결된 문제 (코드 수정 완료)

### 1. File Watcher Service 경로 오류 ✅

**수정 파일**: `UnrealProject/Content/Python/init_unreal.py` (라인 29)

**변경 내용**:
```python
# 이전 (잘못된 경로)
file_watcher_script = mcp_server_dir / "file_watcher_service.py"

# 수정 후 (올바른 경로)
file_watcher_script = mcp_server_dir / "src" / "file_watcher_service.py"
```

---

## ⚠️ 수동 설정이 필요한 부분

### 2. 나무 메시 설정 (필수)

현재 기본 메시가 **Cube**로 설정되어 있어 실제 나무가 표시되지 않습니다.

#### 방법 1: ForestPCGManager에서 메시 설정

1. **언리얼 에디터 열기**
2. **월드 아웃라이너**에서 `ForestPCGManager` 검색
3. **Details 패널**에서 다음 설정:
   - `Tree Mesh` → 나무 Static Mesh 선택
   - `Tree Meshes` → 나무 타입별 메시 설정
     - `pine` (소나무)
     - `oak` (참나무)
     - `birch` (자작나무)
     - `maple` (단풍나무)
     - 등등...

#### 방법 2: PCG Graph에서 메시 설정

1. **Content Browser**에서 생성된 PCG Graph 찾기
   - 경로: `Content/PCG/Generated/PCG_Forest_XXXX`
2. **더블클릭하여 PCG Graph 에디터 열기**
3. **Static Mesh Spawner 노드** 선택
4. **Details 패널**에서:
   - `Mesh Selector Type` → `PCGMeshSelectorWeighted` 선택
   - `Mesh Entries` → 나무 메시 추가
5. **저장 및 Regenerate**

---

## 🚀 테스트 방법

### 1. 에디터 재시작 (권장)

File Watcher Service를 올바르게 시작하기 위해:

```bash
# UE5 에디터 종료 후 재시작
```

### 2. File Watcher Service 수동 시작 (선택)

에디터를 재시작하지 않으려면:

```bash
# 프로젝트 루트 디렉토리에서
MCPServer\scripts\StartFileWatcher.bat
```

### 3. 숲 생성 테스트

#### 방법 A: Cursor/Claude Desktop에서

```
밀집된 소나무 숲
```

#### 방법 B: UE5 에디터에서

1. **ForestPCGManager** 선택
2. **Details 패널**에서 `Generate Forest From NLP` 버튼 클릭
3. 명령어 입력 창에 입력:
   ```
   밀집된 소나무 숲
   ```

---

## 📁 작동 원리

### 파일 기반 통신 흐름:

```
[Cursor/UE5]
  ↓ 명령어 입력
[Intermediate/MCP_Commands/ue5_command.json 생성]
  ↓
[File Watcher Service (Python) 감지]
  ↓
[MCP 서버로 전송]
  ↓
[AI가 파라미터 분석]
  ↓
[mcp_response.json 생성]
  ↓
[UE5가 파일 감지 및 파싱]
  ↓
[PCG 그래프 생성 및 실행]
  ↓
[🌲 숲 생성!]
```

---

## 🔍 디버깅 체크리스트

### 1. File Watcher Service 확인

**Output Log**에서 다음 메시지 확인:

✅ **성공**:
```
🚀 File Watcher Service Started Automatically!
Process ID: XXXX
✅ NLPPCG System Ready!
```

❌ **실패**:
```
❌ File Watcher Service script not found!
```
→ 에디터 재시작 필요

### 2. 명령 파일 확인

명령이 파일로 저장되는지 확인:

```
<프로젝트경로>/UnrealProject/Intermediate/MCP_Commands/ue5_command.json
```

**Output Log**에서:
```
✅ Command sent via file: ...
```

### 3. 응답 파일 확인

File Watcher가 응답을 생성하는지 확인:

```
<프로젝트경로>/UnrealProject/Intermediate/MCP_Commands/mcp_response.json
```

**30초 이상 응답 없으면**:
- File Watcher Service가 실행 중인지 확인
- MCP 서버가 실행 중인지 확인 (Cursor/Claude Desktop)

### 4. PCG 생성 확인

**Output Log**에서:
```
Received forest parameters from MCP
PCG Forest generated with parameters: Density=dense, MinDist=XXX
```

---

## 🎯 예상 결과

### 설정 완료 후:

1. **에디터 시작 시**:
   ```
   🚀 File Watcher Service Started Automatically!
   ✅ NLPPCG System Ready!
   ```

2. **명령 입력 시**:
   ```
   ✅ Command sent via file: 밀집된 소나무 숲
   Received forest parameters from MCP
   PCG Forest generated
   ```

3. **뷰포트에 숲 표시** (메시 설정 완료 시)

---

## ❗ 여전히 문제가 있다면

### 확인 사항:

1. ✅ 에디터 재시작 했나요?
2. ✅ File Watcher Service가 시작되었나요?
3. ✅ 나무 메시를 설정했나요?
4. ✅ PCG Graph가 생성되었나요?
5. ✅ Cursor/Claude Desktop이 실행 중인가요?

### 추가 도움말:

- `빠른_문제해결.md` 참조
- Output Log 전체 내용 확인
- Python 에러 메시지 확인

---

## 📝 다음 개선 사항

1. **자동 메시 다운로드** 구현
2. **UI 개선** - 더 직관적인 설정 패널
3. **실시간 프리뷰** 기능
4. **에러 메시지 개선** - 더 명확한 안내

---

**문서 작성일**: 2025-11-24
**버전**: 1.0
**수정 완료**: `init_unreal.py` 경로 수정
